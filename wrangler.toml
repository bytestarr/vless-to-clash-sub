name = "vless-to-clash-sub"
main = "src/index.js"
compatibility_date = "2026-05-31"
keep_vars = true

# Do not store real tokens or node URLs in this file.
# keep_vars preserves variables configured in the Cloudflare Dashboard during deploys.
# Configure SUB_TOKEN and NODE_* as secrets in the Cloudflare Dashboard.
